name: Deploy Node.js App

on:
  push:
    branches:
     - dev

jobs:
  build-and-deploy:
    runs-on: self-hosted   # 👈 Your own runner instead of GitHub-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install --production

      - name: Build Docker image
        run: |
          docker build -t my-node-app .

      - name: Stop old container (if running)
        run: |
          docker rm -f my-node-app || true

      - name: Run Docker container
        run: |
          docker run -d -p 3000:3000 --name my-node-app -e RAZORPAY_KEY_ID=rzp_test_6bLKr4BVxTpeRI -e RAZORPAY_KEY_SECRET=I8SllVW1cBGgXbIiEXWphUUJ -e MONGO_URI="mongodb+srv://Prince:prince1234@hairlyfdb.ene6ekk.mongodb.net/?retryWrites=true&w=majority&appName=HairlyfDB" -e JWT_SECRET=9d8f7g6H&3jK9rYp$1ZqLmN!vXbTpQeW -e JWT_REFRESH_SECRET="Uv9@eKp!TzN3Lp9yGwVbYxLk#fA7RcEo" -e JWT_RESET_SECRET=Ye7TfGp@1LmNzVxRwQ!Kd9uYxTe2ZpQv -e JWT_EMAIL_SECRET="Pk3RsHv$7NjBgLtKeZxCqAv#XfW9TzUm" -e EMAIL_PASS="imne oirt vike zteo" -e EMAIL_USER=bussieditz@gmail.com -e CLOUDINARY_API_KEY=443167144472138 -e CLOUDINARY_API_SECRET=n6RCTWsSedpWkHsm2DpEyTF7cL8 -e CLOUDINARY_CLOUD_NAME=dudxbexga my-node-app
